# GitHub Pages deployment

ArchitectOS is deployed by `.github/workflows/architectos-pages.yml` using the GitHub Pages Actions deployment flow. The workflow builds `architectos/` and publishes `architectos/dist`.

Expected project Pages URL: `https://sonali426.github.io/ArchitectOS/`.
